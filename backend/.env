# File: backend/.env
# ZEDSON WATCHCRAFT - Environment Configuration
# Developed by PULSEWARE❤️

# MongoDB Configuration
# Replace with your actual MongoDB connection string
MONGODB_URI=mongodb://localhost:27017/zedson_watchcraft
# For MongoDB Atlas (cloud), use format like:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/zedson_watchcraft?retryWrites=true&w=majority

# JWT Secret - Change this to a strong, unique secret in production
JWT_SECRET=zedson_watchcraft_super_secret_key_2024_pulseware_mongodb

# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Database Options
DB_MAX_POOL_SIZE=10
DB_TIMEOUT_MS=30000

# Security Settings
BCRYPT_ROUNDS=10
JWT_EXPIRE=24h

# Application Settings
APP_NAME=ZEDSON WATCHCRAFT
APP_VERSION=2.0.0
DEVELOPER=PULSEWARE